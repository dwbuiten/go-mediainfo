To use, run the build script, and then add this directory
to your GOPATH.

So, to build the example:
    ./build
    cd example
    GOPATH=.. go build

And to run it, either put libmediainfo_go somewhere it can
be seen, or:
    LD_LIBRARY_PATH=../lib ./example <file> <key>
